<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Неактивное состояние -->
    <item android:state_enabled="false" android:drawable="@drawable/bg_btn_auth_disabled"/>

    <!-- Активное состояние (с ripple) -->
    <item android:state_enabled="true" android:drawable="@drawable/bg_btn_auth_enabled"/>
</selector>